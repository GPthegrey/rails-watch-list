<h2>My lists</h2>
<div class="cards">
  <% @lists.each do |list| %>
<div class="card-category logo<%= (1..5).to_a.sample %>.jpeg">
  <%= link_to list.name, list_path(list)%>
  </div>
  <% end %>
  </div>
  <%= link_to 'Create new List', new_list_path, class: 'btn btn-success mt-4' %>
</div>
